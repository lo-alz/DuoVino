CornerLogo from @duovino/design-system. Use via `window.DuoVino.CornerLogo` (bundle loaded from the root `_ds_bundle.js`).

The DuoVino corner mark — a fully filled miniature wine glass that brands the
edge of the layout.

## Examples

### Default

```jsx
() => <CornerLogo />
```
