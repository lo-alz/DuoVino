import React from "react";
import { Hero } from "@duovino/design-system";

export const Default = () => (
  <Hero
    pct={42}
    mastered={118}
    total={280}
    message="Filling steadily — keep closing the gaps below."
  />
);
