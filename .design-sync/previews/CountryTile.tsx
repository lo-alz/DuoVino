import React from "react";
import { CountryTile } from "@duovino/design-system";

export const Spain = () => <CountryTile name="Spain" flag="🇪🇸" topics={14} pct={38} />;
export const France = () => <CountryTile name="France" flag="🇫🇷" topics={22} pct={61} />;
