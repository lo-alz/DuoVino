import React from "react";
import { ProgressBar } from "@duovino/design-system";

export const Low = () => <ProgressBar pct={25} />;
export const Mid = () => <ProgressBar pct={60} />;
export const High = () => <ProgressBar pct={90} />;
