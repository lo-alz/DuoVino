import React from "react";
import { GlossaryItem } from "@duovino/design-system";

/** A Spanish ageing-category term. */
export const Crianza = () => (
  <GlossaryItem
    term="Crianza"
    language="Spanish"
    def="A Rioja ageing category: min. 2 years, at least 1 in oak."
  />
);
