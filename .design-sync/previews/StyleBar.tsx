import React from "react";
import { StyleBar } from "@duovino/design-system";

/** Rioja — predominantly red, with a little white and rosé. */
export const Rioja = () => <StyleBar style={{ red: 85, white: 8, rose: 7 }} />;
/** Mosel — overwhelmingly white, with a sparkling Sekt share. */
export const Mosel = () => <StyleBar style={{ white: 92, sparkling: 8 }} />;
