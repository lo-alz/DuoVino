import React from "react";
import { SettingItem } from "@duovino/design-system";

/** Curriculum settings row. */
export const Curriculum = () => (
  <SettingItem
    icon="🎓"
    title="Curriculum"
    desc="Choose your qualification & filter regions"
  />
);
