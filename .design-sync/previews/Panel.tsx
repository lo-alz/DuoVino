import React from "react";
import { Panel } from "@duovino/design-system";

export const Default = () => (
  <Panel>
    <h3>Rioja</h3>
    <p>
      Altitude and cold Atlantic-influenced nights give a wide diurnal range, so
      ripe Tempranillo keeps its fresh acidity.
    </p>
  </Panel>
);
