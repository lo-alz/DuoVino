import React from "react";
import { StyleBarInline } from "@duovino/design-system";

/** Adelaide Hills — split between white and red styles. */
export const AdelaideHills = () => <StyleBarInline style={{ white: 60, red: 40 }} />;
/** Brunello di Montalcino — entirely red (Sangiovese). */
export const Brunello = () => <StyleBarInline style={{ red: 100 }} />;
